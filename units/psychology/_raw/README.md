# Psychology Raw Intake

Place source exports or original cycle materials here before conversion into engine package JSON.

Expected intake folders:

```text
_raw/cycle-a/
_raw/cycle-b/
_raw/cycle-c/
_raw/cycle-d/
```

Raw materials are not classroom-ready engine packages. Convert them into package JSON files under `units/psychology/cycles/<cycle>/packages/`, then list each package in that cycle's `cycle.manifest.json`.
